# Environment for Binder (mybinder.org) live compute.
#
# When a reader clicks "run" on a code cell (Thebe/Binder live compute), mybinder.org
# builds a kernel from THIS file. Keep it to just the runtime packages the notebooks
# import -- do NOT add the book's build tooling (mystmd/node) here.
#
# Binder prefers a binder/ directory, so this file is used instead of the repo-root
# requirements.txt (which is for building the book locally).
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10
scikit-learn>=1.3
seaborn>=0.12
ipykernel>=6.0
