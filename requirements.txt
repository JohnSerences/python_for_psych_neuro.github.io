# Toolchain for building this book locally.
#
# This is a MyST-native book (Jupyter Book 2). It is configured by myst.yml and
# built with the MyST Document Engine. The `mystmd` PyPI package provides the
# `myst` command and bootstraps a local Node.js runtime on first run.
mystmd>=1.3

# Scientific stack used by the notebooks. You only need these if you want to
# (re-)execute the notebooks locally; the committed notebooks already include
# their outputs, which the site renders as-is.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10
scikit-learn>=1.3
seaborn>=0.12

# Needed to execute notebooks locally (e.g. `jupyter nbconvert --execute` or
# `myst build --execute`).
jupyter-server>=2.0
ipykernel>=6.0
nbconvert>=7.0
